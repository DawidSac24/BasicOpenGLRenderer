- Correct the Layer Transitions
- Change build configuration. Maybe using a json ? The current commands required:
cmake -B build -S . -G Ninja -DCMAKE_TOOLCHAIN_FILE=/home/dawid/Apps/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build
- Update the assetMAnagement system
- Add/Update Serialisation
- Add 'in-file' serialisation, allowing to chose the variables to show in the inspection window, for scripts and maybe components too
  - would have to add something to addapt mui to a variable type
